import { ValidaUsernameDirective } from './valida-username.directive';

describe('ValidaUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
