import { ValidaContraDirective } from './valida-contra.directive';

describe('ValidaContraDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidaContraDirective();
    expect(directive).toBeTruthy();
  });
});
